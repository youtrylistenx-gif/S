# ════════════════════════════════════════════════════════════
#  กดอัปเดตจำนวนผู้อ่าน + รีวิว ผ่านมือถือ ไม่ต้องเปิดคอมเลย
#
#  วิธีใช้ (ทำได้จากมือถือ เปิดผ่านเบราว์เซอร์ ไม่ใช่แอป GitHub):
#  1. เข้าหน้า repo บน github.com
#  2. แตะแท็บ "Actions" ด้านบน
#  3. แตะ "Update Website Data" ทางซ้าย
#  4. แตะปุ่ม "Run workflow" (สีเขียว) แล้วกดยืนยันอีกที
#  5. รอสักครู่ (ประมาณ 20-40 วิ) ตัวเลขคนอ่านและรีวิวในเว็บจะอัปเดตให้เองอัตโนมัติ
#     ไม่ต้อง push อะไรเองเลย ระบบจะ commit + push ให้เสร็จสรรพ
# ════════════════════════════════════════════════════════════

name: Update Website Data

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: ดึงจำนวนผู้อ่านล่าสุดมาแก้ settings.js
        run: node update-reader-counts.mjs

      - name: ดึงรีวิวล่าสุดมาฝังในหน้าอีบุ๊คทั้ง 3 เล่ม
        run: node update-reviews.mjs

      - name: Commit และ push ถ้ามีอะไรเปลี่ยน
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add settings.js ebook-plukfai.html ebook-healjai-read.html ebook-chiwit-read.html
          if git diff --cached --quiet; then
            echo "ไม่มีอะไรเปลี่ยน ไม่ต้อง commit"
          else
            git commit -m "อัปเดตจำนวนผู้อ่านและรีวิวอัตโนมัติ (กดผ่านมือถือ)"
            git push
          fi
